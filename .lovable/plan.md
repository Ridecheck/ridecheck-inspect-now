## Plan: Update hero car to show the rear

1. Generate a new hero image (`src/assets/hero-red-bmw-m3.jpg`) showing the **rear three-quarter view** of a red BMW M3-style performance sedan, parked against a subtle Melbourne skyline backdrop, matching the lighting and premium feel of the current asset.
2. Replace the existing hero image import target in `src/components/landing/Hero.tsx`; keep the current copy, layout and alt text unless the new image composition requires a small crop/object-position tweak.
3. Verify the live preview shows the rear of the car clearly on both desktop (right-hand full-bleed) and mobile.

No other sections or styling will change.